features: b_empty(c_and(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)),c_some(r_primitive(K_pos_garbage-color,0,1),c_primitive(K_pos_obs-obj-col,0)))), n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))), n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)))
Rules:
{ b_empty(c_and(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)),c_some(r_primitive(K_pos_garbage-color,0,1),c_primitive(K_pos_obs-obj-col,0)))) ∧ n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) = 0 ∧ n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) = 0 }  ⇒  {  }
{ b_empty(c_and(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)),c_some(r_primitive(K_pos_garbage-color,0,1),c_primitive(K_pos_obs-obj-col,0)))) ∧ n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) = 0 ∧ n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) = 0 }  ⇒  { ¬b_empty(c_and(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)),c_some(r_primitive(K_pos_garbage-color,0,1),c_primitive(K_pos_obs-obj-col,0)))) ∧ ↑n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) }
{ b_empty(c_and(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)),c_some(r_primitive(K_pos_garbage-color,0,1),c_primitive(K_pos_obs-obj-col,0)))) ∧ n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) = 0 ∧ n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) > 0 }  ⇒  { ¬b_empty(c_and(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)),c_some(r_primitive(K_pos_garbage-color,0,1),c_primitive(K_pos_obs-obj-col,0)))) }
{ b_empty(c_and(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)),c_some(r_primitive(K_pos_garbage-color,0,1),c_primitive(K_pos_obs-obj-col,0)))) ∧ n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) = 0 ∧ n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) > 0 }  ⇒  { ↓n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) }
{ b_empty(c_and(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)),c_some(r_primitive(K_pos_garbage-color,0,1),c_primitive(K_pos_obs-obj-col,0)))) ∧ n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) > 0 ∧ n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) = 0 }  ⇒  { ↑n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) ∧ ↓n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) }
{ b_empty(c_and(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)),c_some(r_primitive(K_pos_garbage-color,0,1),c_primitive(K_pos_obs-obj-col,0)))) ∧ n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) > 0 ∧ n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) = 0 }  ⇒  { ↓n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) ∧ ↑n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) }
{ b_empty(c_and(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0)),c_some(r_primitive(K_pos_garbage-color,0,1),c_primitive(K_pos_obs-obj-col,0)))) ∧ n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) > 0 ∧ n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) > 0 }  ⇒  { ↓n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) ∧ ↓n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) }
{ n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) = 0 ∧ n_count(c_some(r_primitive(K_pos_garbage-at,0,1),c_primitive(K_pos_at,0))) > 0 }  ⇒  { ↑n_concept_distance(c_primitive(K_pos_at,0),r_primitive(K_pos_adj,0,1),c_all(r_inverse(r_primitive(K_neg_obj-at,0,1)),c_primitive(K_pos_holding,0))) }
